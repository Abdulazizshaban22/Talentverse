# Talent Fairs — v2.6
**Realtime Pro + Graph Ranker (LTR+Graph) + Payments Production + Observability + Trust & Safety**

What’s inside:
- `services/api` — Express + Socket.IO (channels, presence, typing) + Prometheus `/metrics` + Stripe webhook verification + admin audit + bans/workflow.
- `services/graph-sync` — optional Neo4j sync (from v2.5).
- `apps/web` — demo pages: `/realtime/pro`, `/admin/safety`, `/admin/audit`, `/admin/bans`.
- `observability/grafana/dashboards` — starter JSON dashboards.
