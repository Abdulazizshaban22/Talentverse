
name: Deploy Production
on: { workflow_dispatch: {}, push: { branches: [main] } }
jobs:
  api-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build API
        run: echo "Build & push to ECR (configure like v8.2)"
  web-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Web
        working-directory: web
        run: |
          npm i
          npm run build
