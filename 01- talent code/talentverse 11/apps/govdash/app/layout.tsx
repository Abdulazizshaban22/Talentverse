
export const metadata = { title: 'TalentVerse — GovDash', description: 'National dashboards' };
export default function Root({ children }: any){
  return (<html lang="ar" dir="rtl"><body style={{margin:0,fontFamily:'system-ui'}}>{children}</body></html>);
}
