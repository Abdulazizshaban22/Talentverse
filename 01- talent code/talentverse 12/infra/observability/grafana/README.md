
# Grafana Government Theme (Branding)
- In Grafana OSS, deep custom branding (logos/colors) is an Enterprise/Cloud feature.
- You can still set `app_title` and default theme via `grafana.ini`, and provision datasources/dashboards as code.
- For more, see docs and Helm values already provided.
