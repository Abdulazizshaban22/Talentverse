export default function Analytics(){
  return <main style={{padding:24}}>
    <h2>Analytics (wired via API /analytics/events)</h2>
    <p>بعد تشغيل الـAPI وOpenSearch وactivity-indexer ستظهر إحصاءات الأحداث هنا.</p>
  </main>
}
